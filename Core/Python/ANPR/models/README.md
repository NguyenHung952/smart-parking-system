# ANPR Model

The file best.pt is required by the ANPR system but is not included in the Git repository.

Required file:
best.pt

Required location:
Core/Python/ANPR/models/best.pt

After downloading the model, make sure the filename is exactly:
best.pt
